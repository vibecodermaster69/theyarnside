"use client";

import { useMemo, useState } from "react";
import { Check, Minus, Plus, X } from "lucide-react";
import type { ColorVariant } from "@/lib/catalogue";

export type VariantSelection = { variant: ColorVariant; quantity: number };

export default function ColorVariantModal({ productName, variants, initialVariantName, onClose, onSelect }: { productName: string; variants: ColorVariant[]; initialVariantName?: string; onClose: () => void; onSelect: (selections: VariantSelection[]) => void }) {
  const firstAvailable = useMemo(() => variants.find((variant) => variant.stockQuantity > 0), [variants]);
  const initialVariant = variants.find((variant) => variant.name === initialVariantName && variant.stockQuantity > 0) ?? firstAvailable;
  const [quantities, setQuantities] = useState<Record<string, number>>(() => initialVariant ? { [initialVariant.name]: 1 } : {});
  const selectedCount = Object.values(quantities).reduce((sum, quantity) => sum + quantity, 0);
  const selections = variants.filter((variant) => (quantities[variant.name] ?? 0) > 0).map((variant) => ({ variant, quantity: quantities[variant.name] }));
  const changeQuantity = (variant: ColorVariant, delta: number) => setQuantities((current) => { const next = Math.max(0, Math.min(variant.stockQuantity, (current[variant.name] ?? 0) + delta)); const updated = { ...current }; if (next) updated[variant.name] = next; else delete updated[variant.name]; return updated; });

  return <div className="variant-layer" role="dialog" aria-modal="true" aria-labelledby="variant-title"><button className="variant-backdrop" onClick={onClose} aria-label="Close color selection" /><section className="variant-modal"><button className="variant-close" onClick={onClose} aria-label="Close color selection"><X size={20} /></button><p className="variant-eyebrow">CHOOSE YOUR COLOR</p><h2 id="variant-title">{productName}</h2><p className="variant-helper">Select one or more colours and choose the quantity for each.</p><div className="variant-options">{variants.map((variant) => { const quantity = quantities[variant.name] ?? 0; const available = variant.stockQuantity > 0; return <div key={variant.name} className={`variant-option ${quantity ? "selected" : ""} ${!available ? "unavailable" : ""}`}><span className="variant-swatch" style={{ backgroundColor: variant.hex }} /><span className="variant-name">{variant.name}<small>{available ? `${variant.stockQuantity} available` : "Out of stock"}</small></span><div className="variant-quantity" aria-label={`${variant.name} quantity`}><button type="button" onClick={() => changeQuantity(variant, -1)} disabled={!quantity} aria-label={`Remove one ${variant.name}`}><Minus size={14} /></button><b>{quantity}</b><button type="button" onClick={() => changeQuantity(variant, 1)} disabled={!available || quantity >= variant.stockQuantity} aria-label={`Add one ${variant.name}`}><Plus size={14} /></button></div>{quantity > 0 && <Check className="variant-check" size={16} />}</div>; })}</div><p className={`variant-status ${selectedCount ? "" : "out"}`}>{selectedCount ? `${selectedCount} item${selectedCount === 1 ? "" : "s"} selected.` : "Choose at least one available colour."}</p><div className="variant-actions"><button type="button" className="btn btn-secondary" onClick={onClose}>Cancel</button><button type="button" className="btn btn-primary" disabled={!selectedCount} onClick={() => onSelect(selections)}>Add {selectedCount ? `${selectedCount} to bag` : "to bag"}</button></div></section><style jsx>{`.variant-layer{position:fixed;inset:0;z-index:1300;display:grid;place-items:center;padding:20px}.variant-backdrop{position:absolute;inset:0;background:rgba(75,58,50,.42)}.variant-modal{position:relative;width:min(520px,100%);max-height:min(720px,calc(100vh - 40px));overflow:auto;padding:30px;background:var(--cream);border:1px solid rgba(75,58,50,.12);border-radius:16px;box-shadow:0 20px 50px rgba(75,58,50,.18)}.variant-close{position:absolute;top:16px;right:16px;color:var(--cocoa)}.variant-eyebrow{margin:0 0 5px;color:var(--coral);font-size:11px;font-weight:700;letter-spacing:.12em}.variant-modal h2{margin:0 0 5px;font:700 28px var(--font-playfair),Georgia,serif}.variant-helper{margin:0 0 20px;color:rgba(75,58,50,.65);font-size:13px;line-height:1.5}.variant-options{display:grid;gap:8px}.variant-option{display:grid;grid-template-columns:28px 1fr auto 20px;align-items:center;gap:10px;padding:11px 12px;color:var(--cocoa);background:var(--white);border:1px solid rgba(75,58,50,.16);border-radius:9px}.variant-option.selected{border-color:var(--coral);box-shadow:0 0 0 2px rgba(224,122,105,.15)}.variant-option.unavailable{opacity:.52}.variant-swatch{width:22px;height:22px;border:2px solid var(--white);outline:1px solid rgba(75,58,50,.25);border-radius:50%}.variant-name{display:grid;gap:2px}.variant-name small{font-size:11px;color:rgba(75,58,50,.65)}.variant-quantity{display:inline-flex;align-items:center;gap:9px;border:1px solid rgba(75,58,50,.18);border-radius:999px;padding:3px 5px;background:var(--cream)}.variant-quantity button{display:grid;place-items:center;width:24px;height:24px;border-radius:50%;color:var(--cocoa)}.variant-quantity button:hover:not(:disabled){background:var(--white)}.variant-quantity button:disabled{opacity:.35;cursor:not-allowed}.variant-quantity b{min-width:14px;text-align:center;font-size:13px}.variant-check{color:var(--sage)}.variant-status{margin:16px 0;color:var(--sage);font-size:13px}.variant-status.out{color:var(--coral)}.variant-actions{display:flex;justify-content:flex-end;gap:10px}@media(max-width:520px){.variant-layer{place-items:end center;padding:10px}.variant-modal{width:100%;max-height:calc(100vh - 20px);padding:25px 14px 16px;border-radius:16px 16px 0 0}.variant-modal h2{font-size:25px}.variant-option{grid-template-columns:27px minmax(0,1fr) auto 18px;padding:12px 9px}.variant-quantity{gap:6px}.variant-quantity button{width:28px;height:28px}.variant-actions{position:sticky;bottom:0;margin:14px -14px -16px;padding:12px 14px;background:var(--cream);border-top:1px solid rgba(75,58,50,.1)}.variant-actions .btn{flex:1}}`}</style></div>;
}
